<USER_REQUEST>
# PROMPT FOR LLM (paste this into a session with code execution / file creation capability)

---

I need you to take the paper content I'm about to provide and produce two output files: a **.docx** and a **.pdf**, generated via LaTeX (or Markdown→LaTeX→PDF/DOCX pipeline, whichever is more reliable in your environment).

## Formatting requirements — follow exactly:

1. **Single column** layout (not double-column) — this is for the initial peer-review submission stage, not final publication layout.
2. **Single line spacing** throughout (no 1.5 or double spacing).
3. **Font:** Times New Roman or Computer Modern, 11–12pt body text.
4. **Standard margins** (1 inch / 2.5cm on all sides).
5. **Color restriction — this is mandatory and applies to every element in the document:**
   - **Use ONLY black text and black/grayscale graphics. No other colors anywhere in the document.**
   - This includes: body text, headings, table borders/shading, figure/chart colors, hyperlinks (if any — render as black, not blue), citation numbers, and any lines or borders.
   - If the source content references colored charts (e.g., "green bars," "blue dashed line"), regenerate or redraw those figures in **black, white, and grayscale only** (use different line styles — solid/dashed/dotted — or grayscale shading/hatching patterns to distinguish data series instead of color).
   - Do not use colored boxes, colored callouts, or colored emphasis text of any kind.
6. Section numbering, headings, and reference list formatting should follow standard academic/finance-journal convention (numbered sections, numbered reference list matching in-text bracket citations).
7. Equations should be properly typeset (LaTeX math mode), not pasted as images or plain text.
8. Tables should use simple black-ruled borders only (no shading, no color).
9. Output both a compiled **PDF** and a **DOCX** version of the final result. If direct LaTeX→DOCX conversion isn't reliable in your environment, generate the DOCX independently from the same source text/structure so both outputs match in content, and flag if any formatting had to differ between the two.

## Before you generate anything:

- If you are missing any part of the paper's final text (any section, table, or figure data), STOP and ask me to provide it rather than fabricating or approximating content.
- If you cannot regenerate a figure in black/grayscale without the underlying data (e.g., you don't have the raw values behind a chart, only a description), tell me which figures need me to supply the underlying data or a source image, rather than guessing at the chart's contents.

## Output:
Provide both files (PDF and DOCX) as downloadable outputs, plus a short list confirming: (a) all color was successfully removed, (b) which figures (if any) could not be fully regenerated and why.

---

**[PASTE YOUR FULL FINAL PAPER TEXT HERE — all sections, tables, and reference list]**
</USER_REQUEST>
<ADDITIONAL_METADATA>
The current local time is: 2026-07-07T13:59:21+05:45.
</ADDITIONAL_METADATA>